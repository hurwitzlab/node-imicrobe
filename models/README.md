These model files (except index.js) were auto-generated using `sequelize-auto`.

```
npm install -g sequelize-auto
npm install -g mysql
sequelize-auto -e mysql -h localhost -d imicrobe -u <user> -x <password>
```
